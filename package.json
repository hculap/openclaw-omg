{
  "name": "@openclaw/omg",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "OPENCLAW_LIVE=1 vitest run --project live",
    "test:live:phase": "OPENCLAW_LIVE=1 vitest run --project live -t",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^4.4.1",
    "yaml": "^2.4.5",
    "zod": "^3.23.8"
  },
  "optionalDependencies": {
    "better-sqlite3": "^11.10.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^25.3.0",
    "@vitest/coverage-v8": "^2.0.5",
    "memfs": "^4.11.1",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=20"
  },
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "clawdbot": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
